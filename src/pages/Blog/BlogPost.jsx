{/*

import React from "react";
import { Link } from "react-router-dom";

function BlogPost({ to, src, by, title, delete_prop }) {
  return (
    <Link to={to} className="post">
      <img src={src} alt="" />
      <h3>By {by}</h3>
      <h1>{title}</h1>
      {delete_prop
        &&
        <button onClick={()=>delete_prop()}>deleteById</button>
        }
    </Link>
  );
}

export default BlogPost;
*/}